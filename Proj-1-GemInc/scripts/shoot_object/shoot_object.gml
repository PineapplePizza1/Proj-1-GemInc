//arg0 = object to be created
//arg1 = direction
//arg2 = speed
var inst = instance_create_layer(x, y, layer, argument0);

//set shooting direction
inst.direction = argument1

//set shooting speed
inst.speed = argument2;